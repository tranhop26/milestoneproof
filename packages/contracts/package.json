{
  "name": "@milestoneproof/contracts",
  "private": true,
  "scripts": {
    "deploy": "node scripts/deploy.mjs",
    "deploy:dry-run": "node scripts/deploy.mjs --dry-run",
    "verify": "node scripts/verify.mjs",
    "e2e:live": "node scripts/e2e.mjs",
    "test": "python -c \"import os, sys, pytest; os.environ['PYTEST_DISABLE_PLUGIN_AUTOLOAD'] = '1'; raise SystemExit(pytest.main(['tests', *sys.argv[1:]]))\" && python scripts/direct_runtime_probe.py"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "genlayer-js": "1.1.8"
  }
}
