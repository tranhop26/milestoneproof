{
  "name": "@milestoneproof/contracts",
  "private": true,
  "scripts": {
    "test": "python -c \"import os, sys, pytest; os.environ['PYTEST_DISABLE_PLUGIN_AUTOLOAD'] = '1'; raise SystemExit(pytest.main(['tests', *sys.argv[1:]]))\""
  }
}
